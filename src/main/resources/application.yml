todos:
  data:
    initialize: true

site:
  author: SpringRunner
  description: What're your plans today?

server:
  error:
    include-message: always
  servlet:
    encoding:
      charset: utf-8
      force: true

logging:
  level:
    todoapp: debug
    org.springframework.web: debug