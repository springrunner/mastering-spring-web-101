todoapp:
  site:
    author: SpringRunner
    description: What're your plans today?
  data:
    initialize: true

spring:
  application:
    name: todo

logging:
  level:
    web: trace
    sql: debug
